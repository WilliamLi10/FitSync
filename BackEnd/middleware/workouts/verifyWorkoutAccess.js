const UpcomingWorkout = require("../../models/UpcomingWorkouts.js");
const mongoose = require("mongoose");

const verifyWorkoutAccess =  (req, res, next) => {
    req.
    next();
};


module.exports = verifyWorkoutAccess;